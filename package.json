{
  "name": "terrasocial-web",
  "private": true,
  "version": "1.0.0",
  "description": "Static frontend build helpers for TERRASOCIAL",
  "scripts": {
    "build": "node scripts/generate-runtime-config.js",
    "vercel-build": "node scripts/generate-runtime-config.js"
  }
}
